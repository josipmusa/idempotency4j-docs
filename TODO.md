# Still to do

One thing, only possible through GitHub's web UI. Delete this file once it is done.

## The repository's social preview

`josipmusa/idempotency4j-docs` → Settings → General → Social preview: upload
`public/og.png`. That is the card that shows when the repository itself is linked, and it
is the same plate the site already serves.
