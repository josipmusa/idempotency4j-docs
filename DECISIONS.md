# Decisions

One line per pick, newest at the bottom: date, what, one-sentence why. Markers the agent writes (see web/RUNBOOK.md): `interview done` · `stage 1 done` · `approaches built` · `stage 2 done` · `sharpen done` · `sub-pages done` · `content done` · `honesty done` · `stage 3 done`.

<!-- Example:
2026-09-21, PLAN confirmed; swapped 3D hero for scrubbed sequence because the client has renders and no clean product photo.
2026-09-21, stage 1 done
2026-09-23, picked approach B "Warm Concrete": hits grounded + honest, A felt generic, C too playful for a 30-year-old firm. Rejected A because..., C because...
-->

2026-09-21, site type is Product, not SaaS: nothing to log into and no sales motion, and the Product anatomy is the only one with a specs slot and a compare slot, which are the two things the tech-lead audience reads.
2026-09-21, dropped the anatomy's social-proof strip and reviews sections: there are no logos, testimonials, ratings or adoption numbers we are permitted to show, and an invented one would fail the never-invent rule.
2026-09-21, added a limits section to the homepage, against the old attempt's choice to hide it in the docs: for the tech lead deciding between this and an in-house table, the published non-goals are the pitch.
2026-09-21, /docs and /learn are rebuilt in our own components rather than Starlight, because a default-shaped docs silhouette behind a designed homepage is part of what made the previous attempt read as generic; search comes from Pagefind at build time.
2026-09-21, adjectives set to exact / physical / unembarrassed, swapping "instrumented" out because it pulls toward dashboards and telemetry, which is what the previous attempt built and why it read as content on a background.
2026-09-21, banned shape recorded: a centred column on dark with hairline rules down both sides. Boss named it as the reason the previous attempt failed, and encore.dev is confirmed as its source.
2026-09-21, the base path is one exported constant and is never written out, because the repository may be renamed once Boss likes the implementation.
2026-09-21, interview done
2026-09-21, stage 1 done
2026-09-21, easing swapped from cubic-bezier(0.2, 0.8, 0.2, 1) to cubic-bezier(0.23, 1, 0.32, 1): the animation skill treats hand-rolled curves as a defect and ships this one as its strong ease-out, so the plan's intent is unchanged and the value is now a sanctioned one.
2026-09-21, the grotesque is Instrument Sans, not Inter: Impeccable's detector flags Inter as one of the faces every AI-generated site converges on, which is exactly the genericness Boss rejected, and the logo's use of Inter does not bind the site.
2026-09-21, GSAP and ScrollTrigger are imported by the scroll-scrubbed moment itself rather than by the layout: approaches B and C have no scrubbed moment and now never download them, which is 44 KB they were paying for nothing.
2026-09-21, content/home.md stays the source of the words and src/data/home.ts mirrors it verbatim as typed data: parsing Markdown into ten differently shaped sections would be fragile, and all three approaches must render byte-identical copy.
2026-09-21, the deployment path and the library version live in site.config.mjs and are derived from the environment, never typed out: renaming the repository changes nothing in the source, which is the constraint Boss set.
2026-09-21, the dev server runs on 4412 rather than the runbook's 4399: a dev server from the abandoned idempotency4j-docs project is still bound to 4399 on this machine and shadows it.
2026-09-21, approaches built

2026-09-21, picked approach C "Foundry Plate": Boss's favourite outright, the hero reads and the annotation plate lands. Rejected A because the duplicate scene in its hero was judged poorly executed and its pinned scroll-scrubbed lease felt "too fast and too slow at the same time"; rejected B because its live lease hero has the same right-hand-animation problem, though its outcomes table is the one thing Boss singled out as clearly good.
2026-09-21, B's outcomes section is carried into C rather than discarded: Boss named it the clearest thing in any of the three, and a table that shows every status for one request is exactly the specs-slot job the Product anatomy wants.
2026-09-21, the duplicate moment is cut from the site, not relocated: Boss rejected the two-call panel with its buttons in both A and C, so the round-6 rule that a moment must earn its place removes it rather than moving a disliked scene somewhere quieter.
2026-09-21, the pinned scroll-scrubbed lease is cut as a concept: scrubbing tied scene time to scroll speed, which Boss read as simultaneously too fast and too slow, and no amount of tuning fixes a clock the visitor is holding.
2026-09-21, the moments are reopened in Stage 3 against the runbook's "moments are not reopened" rule: two of the three were rejected on fit, not craft, and shipping a moment Boss dislikes to protect a process rule would be the wrong trade.
2026-09-21, the support matrix leaves the homepage for the specs sub-page: Boss judged it as belonging elsewhere, and the homepage keeps the limits section, which is the part that actually argues.
2026-09-21, section numbers are dropped in favour of headings alone, and headings are cut to fit: Boss found the numbering off and the titles long, and C's poster scale punishes a long line harder than the other two did.
2026-09-21, the background gains a real constructed field rather than flat black: Boss asked for something built, using it in the hero, and named Inngest as the reference he does not want copied, so the field is geometry of our own.
2026-09-21, C's accent orange and display size are both pulled back: Boss likes the intent and finds the execution loud, which is a tuning note inside the direction rather than a change of direction.
2026-09-21, stage 2 done

2026-09-21, C promoted: its tokens are src/styles/tokens.css, its route is /, and approaches A and B with their token files are deleted along with the duplicate and lease moments and their scripts.
2026-09-21, the token prefix is html[data-world='plate'] rather than the old html[data-approach='c']: the prefix still exists only to outrank Astro's scoped component styles, which compile to a :where() class of equal weight, and the name now says what the world is instead of which of three candidates it was.
2026-09-21, the accent moved from #ff5a1f to #e0562a and the display cap from 9.5rem to 5.5rem: Boss judged both loud, and the quieter orange still clears 5.5:1 on black, so nothing is bought with contrast.
2026-09-21, the background is a drafting lattice with a registration cross at every major intersection, printed once in the hero by a single left-to-right pass of the press: it is plate language rather than an ambient dot field, which is the specific resemblance Boss did not want, and the pass is linear because it depicts a pass across a real sheet.
2026-09-21, the typographic section watermark is deleted: the lattice is the background now, two background systems at once is clutter, and a giant ghost word behind the content is the single most copied device on a dark developer site, so it was also the strongest remaining source of the resemblance Boss flagged.
2026-09-21, every section kicker is gone and the words that mattered moved into the headings: Boss found the numbered sections off, and Impeccable's detector bans a tracked uppercase label above a heading outright as a shape that adds nothing the heading does not already carry.
2026-09-21, the hero annotation is two plates rather than one and steps 01 to 02 once on its own: Boss asked for the examples to cycle, and the library's third case has no annotation to land, so it stays a tab in the change section rather than becoming a plate that cannot perform the gesture.
2026-09-21, the code in the change section settles line by line when you arrive at a tab, 200ms with a 60ms stagger capped at six: Boss asked for the code to fall into place, and Shiki's line spans are boxed by making the <code> a grid, which also avoids the double leading that display:block on them would cause.
2026-09-21, the four outcomes no longer dim their unlit rows: the dimming put two header lines at 1.0:1 against the page, and the row already says it is lit three other ways.
2026-09-21, the nav drops its Get started on a phone and keeps the links instead: below 460px the bar was showing a button the hero repeats two centimetres lower and no navigation at all.
2026-09-21, two cramped-padding findings on the coordinate panel are left standing: its first child is a tab strip, flush to the panel edge by design, with 1.1em of inset on the labels inside it, and suppressing the rule project-wide would hide real cramped padding later.
2026-09-21, the hero plate selector is gone and the three usages cycle on their own: Boss wanted the code animated rather than operated, so the plates change on a press pass - an accent rule crossing the plate in the same direction, at the same constant speed and with the same hairline as the sheet printing behind the hero - and the cycle holds while the pointer rests on it and stops off screen, under reduced motion and in a background tab.
2026-09-21, the plain-Java plate lands two lines as one block: the change there is a call wrapped across two lines, and two accordions opening in a row would read as two edits rather than one.
2026-09-21, the desktop press pass is given a 2px rule, 1.7s of travel and harder marks: at the phone's 1px and 1.1s the same pass across a 1440px sheet read as nothing, and Boss asked for it to land the way it lands on a phone.
2026-09-21, the change tabs shorten their labels below 620px instead of wrapping: three tabs breaking onto a second row read as two controls and a stray.
2026-09-21, every code panel wraps rather than scrolls sideways on a phone, at 0.7rem with the continuation indented: Boss should not have to scroll a code sample horizontally, so the samples were also reset to short lines, and the Maven coordinate got the same treatment because it had the same defect.
2026-09-21, the copy button confirms with a drawn check and a live region, and falls back to a textarea when navigator.clipboard is missing: nothing was confirming because the clipboard API does not exist outside a secure context, which is every phone opening the site over the LAN.
2026-09-21, footer links are no longer stretched to their column: a flex item filling the column gave every link the underline of the longest one, and NOTICE is set as Notice because an all-caps filename beside Apache License 2.0 read as shouting.
2026-09-21, the logo mark animates on hover: the two chevrons are a request and its retry, so the retry slides into the first and is gone and one request reaches the stop bar - the mark saying what the library says, on a fine pointer only, transform and opacity.
2026-09-21, on a phone the hero's moment comes before the dependency panel: stacked in source order the panel put the plate 620px down a 664px screen, so the only moment in the hero was never on screen and its cycle never started - the coordinate is one panel lower and the closing section carries it again.
2026-09-21, the cycle starts when enough of the plate is on screen to read a change rather than at a fraction of it: a 0.4 intersection threshold on a plate that is barely a quarter visible while the hero is read meant a phone never saw it cycle at all.
2026-09-21, the plain-Java plate shows the real builder setup with the engine call arriving into it: every line on it used to be an arriving line, so the snippet read as code that had been cut off rather than as a change landing in a method.
2026-09-21, the deck is pinned to its tallest plate by measurement: the grid cell took the height of the tallest plate as laid out, and a plate whose arriving lines are still collapsed is a row shorter, so the hero grew and shrank on every change.
2026-09-21, a hold stops the next change and never the landing under way: resting a pointer on the plate mid-change cancelled the pending landing and left it standing with its arriving lines collapsed.
2026-09-21, the hold is bound only for a pointer that can rest on something: a touch fires pointerenter too, and a hold that is never released is a cycle that never runs again.
2026-09-21, the request line in the outcomes moment wraps on a phone: its panel could not shrink, so the tail of the Idempotency-Key was clipped with no way to scroll to it.
2026-09-21, the logo keeps the hover gesture and is not assembled from fragments: at 24px in the nav an assembly reads as a glitch, it needs scale and a held moment to work, and the plan allows two moments - the hero plate and the four outcomes - both of which say something the logo cannot.
2026-09-21, the plate's registration marks are kept to the sheet's margin: tiled across the whole field an accent cross landed on the type in the hero copy, struck through a word mid-sentence, and a mark a press aligns by sits outside the print anyway.
sharpen done
2026-09-21, the docs and learn prose lives in content/docs and content/learn as Markdown, loaded by Astro content collections that reach out of src/: the repository's rule is that content/ is the only source of words, and a collection that reads it keeps that true for thirty-three pages as well as for the homepage.
2026-09-21, the reference is rebuilt in our own components rather than ported with Starlight's: the four things the content needed - asides, build-tool tabs, numbered steps and a support matrix - are four small components, against a docs framework whose default silhouette is what made the previous attempt read as generic.
2026-09-21, Markdown runs through Sätteri with four plugins rather than remark: Astro 7 makes Sätteri the default processor, it carries directive support natively for the content's existing ::: asides, and the two link and version plugins port across almost unchanged from the old project.
2026-09-21, __VERSION__ in the content renders from site.config.mjs, in prose as well as in code: in a sentence Markdown reads the token as emphasis before a plugin sees it, which is why it looked like it worked until the first coordinate was quoted in a line of text.
2026-09-21, the reading order of the reference is a listed order in src/data/docs.ts, not the file system: the order is the content's - what you read first, then the model, then the adapters - and the build fails on a page that has no place in it.
2026-09-21, a page's own h2s are its spine in the right margin and the reference index sits in the left, each appearing only when there is room: at 1024px the index arrives, at 1280px the spine, and below that the index is a strip above the page rather than a column between the reader and the prose.
2026-09-21, the prose does not reveal, only the page header does: a reference page is not a section entrance, and a body taller than the screen never satisfies a fractional threshold, so it sat invisible until something else scrolled it into view.
2026-09-21, search is Pagefind, indexed from the built pages after the build: nothing to run, nothing to keep in sync, and the field removes itself when there is no index rather than pretending to search.
2026-09-21, the plate label above a page title is a breadcrumb rather than a tracked-caps kicker: the detector names that shape as the default AI hero eyebrow, and where the label is a trail you can walk back up it has a job instead.
2026-09-21, the measure is 58ch: ch undercounts a proportional face, so the 74ch that looked right measured 88 characters a line, past the point where the eye loses the line it is on.
2026-09-21, tables carry no border around the outside: the head rule and the row rules are the drawing, an outer border is framing, and it put the first cell flush against a line.
2026-09-21, the learn index sets each article's question in sentence case: it is the largest text on the row, and a question long enough to be worth asking is too long to read in capitals.
sub-pages done
2026-09-22, the reference index folds by group, with the group you are reading in open and every group name always on screen: all twenty-eight titles at once ran 1221px in a 756px column, so Operating and half of Reference were below the fold of a column whose scrollbar is invisible until hover - which reads as a missing section rather than a scroll.
2026-09-22, the prose rhythm and measure apply inside a step and a tab panel, not only to the top level of .prose: written as a child-only rule, a paragraph in a tab panel took no gap above it and no measure, so it sat on the strip's rule and ran the full width of the box.
2026-09-22, the quickstart's first step is a two-dimensional picker - build tool across the top, store below it - generating all nine coordinates from site.config.mjs: the step used to print the JDBC dependency with "pick one storage backend" in a comment, which leaves the reader to find the artifact id for the store they actually run.
2026-09-22, there is one next on a reference page and it is the reading order's: four pages carried their own "Next" section as well, and on the quickstart the two named different pages.
2026-09-22, the Start group reads quickstart, what it does, requirements: the homepage leads to the quickstart, so what you ran is the question the next page should answer, and the support matrix is a check you make once rather than the second thing you read.
2026-09-22, the copy button sits as a tab on the code plate's top edge instead of over its top-right corner: laid over the corner it covered the first lines of every snippet on a touch screen, where it cannot wait for a hover and the code is wider than the column anyway - which read as a block cut off at the corner.
2026-09-22, the quickstart's build strip is Maven and Gradle, and the BOM is a note under the block: a third "Maven + BOM" tab was the only label in the site that matched no other build-tool strip, so the choice could not travel, and it was also the one that made the row wrap on a phone.
2026-09-22, a question and its choices stay on one line down to 360px, insets giving way rather than the label taking a row of its own: a label above its own strip reads as two separate controls.
2026-09-22, the margins are stretched to the height of the prose beside them: a sticky index can only travel inside its own box, and started at the top of the row the box was as tall as its own contents, so the reference index and the on-this-page spine scrolled away with the first screen.
2026-09-22, a code block in the docs wraps on a phone instead of scrolling sideways, breaking inside a coordinate when it has to: the home page's panels already wrapped, and a snippet you have to drag sideways to finish is a snippet you read half of. No hanging indent on the continuation - the highlighter's lines cannot be set as blocks without losing the blank lines inside a snippet - so the line's own indent is what carries the shape.
2026-09-22, the site no longer describes itself or how it was made: the reference index opened with a count of its own pages and an assurance about our editorial process, the 404 blamed a rebuild only we know happened, and the homepage's specs section carried a note explaining why it had moved - all of it written for Boss reading a draft, none of it for someone deciding whether to use the library.
2026-09-22, the page count is out of the prose entirely rather than rendered from the reading order: "twenty-eight pages" is a fact about the documentation, not about idempotency, and a reader who wants the shape of the reference is already looking at the contents page underneath the sentence.
2026-09-22, every [mock] marker is resolved rather than removed: the six were all section headings I wrote, "How it works" and "What this is not" are the library's own, and the other four name what the section shows, so nothing was invented and nothing needed the client.
2026-09-22, QUESTIONS.md is closed with a statement rather than left as an empty table: the never-invent list turned up nothing the library does not answer, and an empty table reads as unfinished rather than as checked.
2026-09-22, the truth check ran against the Java sources as well as the README where the README does not go: the Redis poll interval, retention grace, purge bounds and the four validation messages quoted on the troubleshooting page are only in the code, and all seven matched.

2026-09-22, the accent is no longer a syntax colour: it was bound to Shiki's keyword token, and Java's grammar calls `void`, `=`, every annotation and every type name a keyword, so most of a snippet came out orange and the one line the snippet exists to show was indistinguishable from the code around it.
2026-09-22, code is printed in four levels of the one ink rather than coloured by token type: full paper-white for the structural words, mid ink for the body, one step down for strings, dim for punctuation and comments - a plate is printed, and the lowest of the five still clears 5.4:1 on black.
2026-09-22, `--astro-code-foreground` and `--astro-code-background` are defined where `--astro-code-color-text` and `-background` were: those are the older Shiki names, Astro emits the first pair, so every span Shiki gave no token type was inheriting its container's colour instead of taking the palette. The rule in base.css keyed on `--shiki-color-text` was dead for the same reason.
2026-09-22, the change section marks its added lines the way a diff does, in the accent with a `+` in the margin: the `added` indices were already in the data and unused, the hero plate already speaks that language, and with the accent spent nowhere else in a snippet the annotation is now the one thing the eye lands on.
2026-09-22, the quickstart's question labels stop being chips: Build tool and Store were mono, uppercase, tracked, --ink-3 and inset exactly like an unselected choice beside them, so each row read as four equal cells with no way to tell the question from the answers. They are the body face in sentence case now, right-aligned in a fixed stub column with the strip's keyline beside them, which makes the two rows one table.
2026-09-22, a picked choice is stamped with a 2px accent rule under it, on every strip on the site: ink and ground alone is a difference you can only see by comparing two chips against each other, and in a row that also contains a label there is nothing to compare against.
2026-09-22, the "Source: README ..." line is off every reference page: it was the loudest remaining piece of the site talking about how it was made, printed under the lede of twenty-six pages. The provenance stays in front matter, where it is what the truth check reads at the next release, and a reader deciding whether to use the library never sees where our sentences came from.
content done

2026-09-22, the favicon was still Astro's scaffold logo: it is the site's own mark now - the two chevrons and the stop bar - written with the accent's value rather than a token, because a file in public/ is served as it is and never reaches the stylesheet. The .ico is dropped; every browser that matters takes the SVG.
2026-09-22, the OG image is drawn as a plate rather than photographed or generated from a template: the site has no photography and the direction is a printed sheet, so the card is the lattice, the mark, the claim at poster scale and the two facts a developer scanning a link wants - the licence and what it runs on. It is rasterised through the real faces, because SVG is not an OG format anywhere that matters.
2026-09-22, the largest layout shift on the site was the reference index deciding its own fold in script: the server rendered the column open - the search field and all six group names - and the script collapsed it on a phone once it had measured the viewport, pulling the whole page up after paint. The narrow default is the stylesheet's now and the attribute only records a reader's choice, which took the docs pages from CLS 0.306 to 0.013 and from 82 to 97.
2026-09-22, Lighthouse blamed the font for that shift and it was wrong: the mono face was loading at the same moment, so the trace attributed the movement to it. Two rounds went into metric-matched fallbacks and a mono preload before the learn articles - same prose, same code blocks, no index, CLS 0 - showed what the docs pages actually had that they did not.
2026-09-22, the faces are declared in src/styles/fonts.css rather than imported from Fontsource: the content is English and the stylesheets pull latin-ext, vietnamese, cyrillic and greek with them, ten files where three are used, and a stylesheet in node_modules cannot be told font-display. All three are preloaded.
2026-09-22, the mono face is font-display: optional and has no metric-matched fallback: a fallback declared with local() cannot be relied on - the one for Menlo resolved to nothing in testing and went to error, leaving the stack unadjusted - so the mono face does not swap at all rather than swapping into a fallback that does not match. The two sans faces keep swap, and their fallbacks are measured, not guessed.
2026-09-22, the search field is on the page from the first paint and its 45 KB index is fetched on first focus: it used to ship hidden and appear once the bundle answered, and every reader of every reference page was made to download an index most of them never search.
2026-09-22, --ink-3 moved from #78756e to #807d76: the level still carries text - unselected tab labels, the mono captions - and most of it sits on --bg-raised rather than on the sheet, where it measured 4.31:1 and missed the bar. It clears it on both grounds now without moving the level's character.
2026-09-22, Lighthouse across nine pages: performance 97-99, accessibility 100, best practices 100, SEO 100, worst CLS 0.013, zero console errors. One h1 per page, no heading level skipped, reduced motion gives a real static frame with Lenis off and every arriving line already landed.
honesty done

2026-09-22, the site takes the `idempotency4j-docs` name rather than shipping as `-site` or `-web`: the abandoned repo under that name was serving publicly and would have competed with this one in search, and "docs" is the word a reader would type while "site" and "web" are only our words for our own work - the same thing the last pass took out of the pages.
2026-09-22, the deployment is GitHub Pages under the repository's own path with no custom domain: the kit's runbook says Cloudflare and a domain, and this project's CLAUDE.md overrides it. A library's documentation living on the same account as the library, at no cost and with no renewal, is one fewer thing to keep alive.
2026-09-22, the workflow names no repository and no base path: `site.config.mjs` reads `GITHUB_REPOSITORY` on Actions and the checkout directory locally, so the rename that just happened needed no edit and the next one will not either.
2026-09-22, a newer push waits for a running deployment instead of cancelling it: a half-published site is worse than a slow one, and nothing here is urgent enough to be worth the risk.
2026-09-22, the legal footer was already in place and needed nothing: the Apache 2.0 licence, the notice and the copyright line are there, and there is no AI-imagery disclosure to make because the site ships no imagery at all - the only raster file is the share card, drawn as a plate.
2026-09-22, the record-lifecycle diagram is flagged in the page rather than drawn yet: it is the one place on the site where a drawing beats the prose, because three states and five named edges have to be held at once, and it goes above the parts list so the plate comes first and its callouts under it. The request-outcomes diagram is not coming - the homepage moment and the HTTP reference already tell that twice.
2026-09-22, the Lettuce link points at the GitHub repository rather than lettuce.io: the project's certificate expired on 20 September 2026, so the only external link on the site that a reader would actually click from a storage page was handing them a browser security warning.
