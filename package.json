{
  "name": "idempotency4j-docs",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check:links": "node scripts/check-links.mjs",
    "check:overflow": "node scripts/check-overflow.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.42.1",
    "astro": "^7.3.3"
  },
  "allowScripts": {
    "esbuild": true
  },
  "description": "Documentation and articles for idempotency4j",
  "private": true,
  "devDependencies": {
    "playwright": "^1.63.0",
    "vitest": "^5.0.1"
  }
}
